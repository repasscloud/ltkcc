<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="LTKCC.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Settings">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="12">

            <Grid ColumnDefinitions="160,*,Auto" ColumnSpacing="12">
                <Label Text="Base Directory" VerticalTextAlignment="Center" />
                <Entry Grid.Column="1" Text="{Binding BaseDir}" IsReadOnly="True" />
                <Button Grid.Column="2" Text="Open" Clicked="OnOpenBaseDirClicked" />
            </Grid>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
